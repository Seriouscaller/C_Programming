#include <stdio.h>
#include <stdlib.h>

int enumerations(){
    enum boolean{ NO, YES};

    enum escapes{ BELL = '\a', BACKSPACE = '\b', TAB = '\t', NEWLINE = '\n', VTAB= '\v', RETURN = '\r',};

    //Setting the startingpoint of the enum yourself.
    enum months{ JAN = 1, FEB, MAR, APR, MAY, JUN, JUL, AUG, SEP, OKT, NOV, DEC};

return 0;
}

